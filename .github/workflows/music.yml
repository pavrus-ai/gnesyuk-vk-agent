on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:
name: Music Agent
  run: python music_agent.py
  env:
    VK_ACCESS_TOKEN: ${{ secrets.VK_ACCESS_TOKEN }}
    VK_OWNER_ID: ${{ secrets.VK_OWNER_ID }}
